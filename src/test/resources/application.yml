# src/test/resources/application.yml
spring:
  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    cache: false

logging:
  level:
    org.springframework: INFO
    org.hibernate.SQL: INFO
